name: Email Step Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 23f3004096@ds.study.iitm.ac.in
        run: echo "Hello from the exam email step!"
